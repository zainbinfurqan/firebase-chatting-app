
import { StyleSheet } from 'react-native'
import constants from '../../config/constants'
export default StyleSheet.create({

    main: { flex: 1, padding: constants.screenMargin },
    // headingText: { alignSelf: 'center' }
})